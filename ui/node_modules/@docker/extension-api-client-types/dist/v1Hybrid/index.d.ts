import { DockerDesktopClient as DockerDesktopClientV0 } from '../v0';
import { DockerDesktopClient as DockerDesktopClientV1 } from '../v1';
export * from '../v1';
export { Backend as BackendV0, RequestConfig as RequestConfigV0, ExecResult as ExecResultV0, } from '../v0';
/**
 * When we moved from the v0 to v1 schema, we made sure
 * window.ddClient satisfied *both* interfaces. This combined type
 * describes the resulting API. We should delete it when we stop providing
 * the v0 API.
 */
export interface DockerDesktopClient extends DockerDesktopClientV0, DockerDesktopClientV1 {
}
